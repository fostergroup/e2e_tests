var RepresentativesPage = function () {
    //get representative information:
    var repr_info = element(by.id('representatives-tab'));
    this.repr_info = repr_info;
};
module.exports = RepresentativesPage;